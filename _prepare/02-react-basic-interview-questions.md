# React Interview Questions — Basic

## Core Concepts
1. `state` vs `props`
2. `React.createElement` vs JSX — what happens under the hood
3. What is the Virtual DOM and how does React use it to improve performance?
4. Purpose of the `key` prop, and why React warns about missing/incorrect keys
5. Controlled vs uncontrolled input components
6. `React.Children.map` vs a regular array `.map()`
7. Purpose of `displayName` on a component
8. Functional vs class components — key differences
9. Different types of Hooks in React and what each is for
10. Purpose of `PropTypes` for type checking

## Rendering & Data Flow
11. Render dynamic HTML data from a data source
12. Pass data from a parent component to a child component
13. Call a parent component's method from a child component
14. Access a DOM element directly (`useRef`)
15. Bind an array/array of objects to a dropdown
16. Bind an array/array of objects to a radio button group
17. Loop over an array/array of objects in JSX
18. Display keys and values of an object in a loop
19. Conditionally render an element or text
20. Change styles based on a condition
21. Show/hide data based on a condition

## Hooks Basics
22. `useState` — building a simple counter
23. `useEffect` — call a method when a component first renders
24. `useEffect` vs `useLayoutEffect`
25. Which class lifecycle methods does `useEffect` replace?
26. Re-render a component when a value changes
27. Add data into a `useState` array in a functional component
28. Call a method immediately after state updates / after a re-render

## Small Build Exercises
29. Create a search text-box filter
30. Create a counter component using `useState`
31. Create a counter component using `useReducer`
32. Create a lazy-loaded component
33. Create a pure component
34. Create a controlled and an uncontrolled version of the same component
35. Display the value a user selects from a dropdown/radio group in another text box
36. Character-remaining counter for a `<textarea>` using `useRef`
