# ✅ Frontend Interview Master Blueprint 

---

## 1️⃣ Core JavaScript — **Conceptual / Theory (L1–L2)**

These test **fundamentals + mental model**.

### Language Basics

* Data types (primitive vs non-primitive)
* `null` vs `undefined`
* `typeof`, `NaN`
* Pass by value vs pass by reference
* Dynamic vs static typing
* Strict mode & why it exists

### Scope, Hoisting & Execution

* Hoisting (var vs let/const)
* Temporal Dead Zone (TDZ)
* Function vs block scope
* Execution context & call stack

### Functions & Objects

* Closures (real-world use cases)
* `this` keyword (all scenarios)
* Arrow functions limitations
* IIFE & use cases
* Prototype & prototypal inheritance
* Generator functions

### Objects & Immutability

* Object creation methods
* Shallow copy vs deep copy
* `Object.freeze()` vs `Object.seal()`
* `Object.keys()` vs `values()` vs `entries()`
* Destructuring, spread & rest operators

---

## 2️⃣ Core JavaScript — **Coding / Logic (MOST ASKED)**

These are **non-negotiable**.

### Functions & Utilities

* Debounce (from scratch)
* Throttle (from scratch)
* `once(fn)` implementation
* Memoization function
* Currying (normal + infinite)
* Callback → Promise conversion

### Arrays & Strings

* Flatten nested arrays (no `flat`)
* Remove duplicates (no `Set`)
* First non-repeating character
* First repeating character
* Sum without loops (`reduce` / recursion)

### Polyfills (Very Important)

* `call`, `apply`, `bind`
* `map`, `filter`, `reduce`

---

## 3️⃣ Async JavaScript & Event Loop — **Deep Dive**

This separates **average vs strong** candidates.

* Event loop (call stack, microtasks, macrotasks)
* `setTimeout` / `setInterval`
* Promise lifecycle (`then`, `catch`, `finally`)
* `async/await` vs Promises
* Output-based async puzzles
* `Promise.all`, `allSettled`, `race`, `any`
* Cancellable fetch (`AbortController`)
* Memory leaks & prevention
* Web Workers (off-main-thread execution)

---

## 4️⃣ DOM & Browser — **Frequently Ignored, Highly Valued**

### Events

* Event bubbling vs capturing
* Event delegation (implementation)
* `event.target` vs `event.currentTarget`
* Detect click outside component

### UI Utilities

* Build modal from scratch (ESC, focus trap)
* Infinite scroll (race conditions)
* Drag & drop (vanilla JS)
* Intersection Observer use cases

### Browser Fundamentals

* DOM vs BOM
* Critical Rendering Path
* Reflow vs repaint
* Browser caching
* CORS (why & how)

---

## 5️⃣ React Fundamentals — **Theory + Internals**

### Core Concepts

* Virtual DOM & reconciliation
* React Fiber architecture
* Keys & rendering bugs
* Controlled vs uncontrolled components
* Functional vs class components
* Strict Mode (double render behavior)

### Hooks (In Depth)

* `useState`, `useEffect`, `useRef`
* `useMemo` vs `useCallback`
* `useContext`, `useReducer`
* Custom hooks & abstraction patterns
* Cleanup patterns & memory leaks

---

## 6️⃣ React — **Machine Coding / Live Rounds**

### Very Common Tasks

* Counter (start / pause / reset)
* Stopwatch (live timer)
* Search with debouncing
* Todo app (optimize re-renders)
* Pagination logic
* Dynamic form from JSON
* Controlled vs uncontrolled input demo

### Performance-Focused

* Prevent unnecessary re-renders
* Render 10k+ items efficiently
* Lazy loading (manual + React.lazy)
* Infinite scroll with APIs
* Modal management system

---

## 7️⃣ State Management & Architecture — **Senior Signal**

* Props vs state vs derived state
* Lifting state vs Context
* Context vs Redux vs Zustand
* Global error handling strategy
* Sharing state without Redux
* Handling race conditions in React
* API loading / error / empty states

---

## 8️⃣ Performance Optimization — **Real-World Judgment**

* Debouncing vs throttling (when to use what)
* Memoization strategies
* Immutability & state updates
* Code splitting (beyond syntax)
* Bundle size optimization
* Tree shaking
* Virtualization
* Optimistic UI
* Performance profiling

---

## 9️⃣ Rendering Strategies (React + Next.js)

* CSR vs SSR vs SSG vs ISR
* SEO trade-offs
* Lazy loading components vs data
* Image optimization (WebP, AVIF)
* Suspense & loading strategies

---

## 🔟 Storage, Security & Networking

### Storage

* LocalStorage vs SessionStorage vs Cookies
* IndexedDB vs SessionStorage
* Custom storage wrapper with expiry

### Networking

* REST vs GraphQL
* Fetch vs Axios
* Interceptors
* WebSockets vs SSE

### Security

* Authentication vs Authorization
* XSS, CSRF basics
* Secure token handling
* Why CORS exists

---

## 1️⃣1️⃣ DSA for Frontend (YES, It Matters)

* Two Sum (optimized)
* Sliding window problems
* String manipulation
* LRU Cache (concept + code)
* Task queue with concurrency
* Median from stream (two heaps)
* Valid parentheses
* Deep equal comparison
* `set(obj, path, value)` utility

---

## 1️⃣2️⃣ Testing & Quality

* Unit vs integration vs E2E testing
* Jest, RTL, Playwright
* Testing hooks & async logic
* Debugging production issues
* Handling flaky tests

---
